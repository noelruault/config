[alias]
    pat = !git add -N . && git add --patch # 'git pat' = !git add -N . && git add --patch
    aliases = !git config -l | grep alias | cut -c 7-

# Resource: # https://gist.github.com/pksunkara/988716
[url "git@github.com:"]
	insteadOf = https://github.com/
[user]
	name = Noel Ruault
	email = noelou.93@gmail.com
